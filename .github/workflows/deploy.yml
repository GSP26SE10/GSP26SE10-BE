name: Auto Deploy to Local Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull code
        uses: actions/checkout@v3

      - name: Build project
        run: |
          cd BookfetSystem/BookfetSystem.API
          dotnet publish -c Release -o publish

      - name: Kill old API (force by port)
        shell: powershell
        run: |
          run: |
          Write-Host "Stopping all dotnet processes..."
          taskkill /F /IM dotnet.exe /T 2>$null

           if ($LASTEXITCODE -eq 0) {
          Write-Host "Killed existing dotnet processes"
          } else {
          Write-Host "No dotnet process running"
          }

      - name: Start new app
        shell: powershell
        run: |
          $projectDir = "$env:GITHUB_WORKSPACE/BookfetSystem/BookfetSystem.API"

          Start-Process -FilePath "dotnet" `
            -ArgumentList "run --configuration Release" `
            -WorkingDirectory $projectDir
